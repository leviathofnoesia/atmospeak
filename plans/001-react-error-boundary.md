# Plan 001: Add React error boundary to prevent full UI crash> **Executor instructions**: Follow this plan step by step. Run every> verification command and confirm the expected result before moving to the> next step. If anything in the "STOP conditions" section occurs, stop and> report — do not improvise. When done, update the status row for this plan> in `plans/README.md`.>> **Drift check (run first)**: `git diff --stat 1bb6795..HEAD -- src/App.tsx src/main.tsx`> If any in-scope file changed since this plan was written, compare the> "Current state" excerpts against the live code before proceeding; on a> mismatch, treat it as a STOP condition.## Status- **Priority**: P1- **Effort**: S- **Risk**: LOW- **Depends on**: none- **Category**: correctness- **Planned at**: commit `1bb6795`, 2026-06-10## Why this mattersThe React app has no error boundary. Any `throw` in any render — a missed null check in `HomePanel`, a failed API call in `SettingsPanel`, or a type coercion bug in `OverlayWindow` — crashes the entire UI to a white screen. The user sees nothing and must restart the app. A single error boundary wrapping the hub content catches these and shows a recovery message instead.## Current state- `src/App.tsx` — the entire 2800+ line app component. All panel renders are inline inside `<main className="app-shell">`. No error boundary exists anywhere in the project.- `src/main.tsx` — the React entry point. Renders `<App />` directly.Excerpt from `src/App.tsx:998-1000` (the hub render start):```tsx  return (    <main className="app-shell">      <section className="top-strip" aria-label="Application status">```The repo uses React 19 and has no existing error boundary utility. The test style uses vitest + @testing-library/react (see `src/components/StatusLed.test.tsx`).## Commands you will need| Purpose   | Command                  | Expected on success ||-----------|--------------------------|---------------------|| Install   | `bun install`           | exit 0              || Typecheck | `npx tsc --noEmit`       | exit 0, no errors   || Tests     | `bun run test`           | all pass            || Rust tests| `& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --manifest-path src-tauri/Cargo.toml` | all pass |## Scope**In scope**:- `src/components/ErrorBoundary.tsx` (create)- `src/components/ErrorBoundary.test.tsx` (create)- `src/App.tsx` (wrap hub and overlay in error boundary)**Out of scope**:- `src/App.css` — no style changes needed beyond a small inline error style- `src-tauri/` — no Rust changes- `src/lib/api.ts` — no API changes## Git workflow- Branch: `advisor/001-react-error-boundary`- Commit style: imperative, no conventional-commits prefix (matches existing: "Add escape cancel for dictation", "Improve spoken dictation cleanup")- Do NOT push or open a PR.## Steps### Step 1: Create ErrorBoundary componentCreate `src/components/ErrorBoundary.tsx` with a class component error boundary that:- Catches errors in its children's render- Displays a minimal "Something went wrong" message with a "Reload" button that calls `window.location.reload()`- Logs the error to `console.error`- Exports `ErrorBoundary`The component should accept `children: ReactNode` and an optional `fallback?: ReactNode` prop so callers can customize the error UI.**Verify**: `npx tsc --noEmit` → exit 0### Step 2: Create ErrorBoundary testCreate `src/components/ErrorBoundary.test.tsx` following the pattern in `src/components/StatusLed.test.tsx`:- Test: renders children when no error- Test: renders fallback UI when a child throws during render- Test: fallback UI contains a "Reload" buttonUse `vitest` + `@testing-library/react`. The test setup file is `src/test/setup.ts`.**Verify**: `bun run test` → all tests pass including new ones### Step 3: Wrap hub content in error boundaryIn `src/App.tsx`, import `ErrorBoundary` and wrap the return value of the main `App` component's JSX (the `<main className="app-shell">` block, lines ~998-1231) inside an `<ErrorBoundary>`.The wrap should look like:```tsxreturn (  <ErrorBoundary>    <main className="app-shell">      ...existing content...    </main>  </ErrorBoundary>);```### Step 4: Wrap overlay content in error boundaryIn `src/App.tsx`, find the `OverlayWindow` function component (starts around line 1249). Wrap its `<main className="overlay-shell">` return in a separate `<ErrorBoundary>` with a shorter fallback message suitable for the floating overlay (e.g., "Atmospeak encountered an error. Tap to reload.").**Verify**: `npx tsc --noEmit` → exit 0### Step 5: Verify all tests pass**Verify**: `bun run test` → all pass**Verify**: `& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --manifest-path src-tauri/Cargo.toml` → all pass## Test plan- New test file: `src/components/ErrorBoundary.test.tsx`  - Happy path: renders children without error  - Error path: child component throws, boundary shows fallback with reload button- Existing tests in `src/components/RecorderOverlay.test.tsx` and `src/components/StatusLed.test.tsx` must continue passing (regression check)- Verification: `bun run test` → all pass, including the 3 new ErrorBoundary tests## Done criteria- [ ] `npx tsc --noEmit` exits 0- [ ] `bun run test` exits 0; new tests for ErrorBoundary exist and pass- [ ] `grep -rn "ErrorBoundary" src/App.tsx` shows both the hub and overlay wraps- [ ] `src/components/ErrorBoundary.tsx` and `src/components/ErrorBoundary.test.tsx` exist- [ ] No files outside the in-scope list are modified (`git status`)- [ ] `plans/README.md` status row updated## STOP conditionsStop and report back (do not improvise) if:- The code at `src/App.tsx:998-1000` doesn't match `<main className="app-shell">` (the codebase has drifted).- A step's verification fails twice after a reasonable fix attempt.- The fix appears to require touching `src-tauri/` files or `src/lib/api.ts`.- You discover React 19's error boundary API differs from React 18's class-component pattern (unlikely but possible if a breaking change exists).## Maintenance notes- If the hub or overlay structure changes (the `<main>` wrappers move), the error boundary wraps should follow.- Reviewers should check that the error boundary catches both render errors and lifecycle errors in children (class component boundaries catch both by default).- Follow-up: a future plan could add a "Send feedback" link inside the error boundary fallback, connecting to the existing `submitFeedback` API.