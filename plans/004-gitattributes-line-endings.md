# Plan 004: Add .gitattributes to normalize line endings and eliminate CRLF warnings> **Executor instructions**: Follow this plan step by step. Run every> verification command and confirm the expected result before moving to the> next step. If anything in the "STOP conditions" section occurs, stop and> report — do not improvise. When done, update the status row for this plan> in `plans/README.md`.## Status- **Priority**: P2- **Effort**: S- **Risk**: LOW- **Depends on**: none- **Category**: dx- **Planned at**: commit `1bb6795`, 2026-06-10## Why this mattersEvery `git diff` and `git status` on this Windows repo produces 20+ lines of "LF will be replaced by CRLF" warnings. This noise hides real issues, annoys developers, and creates unnecessary friction. A `.gitattributes` file with explicit line-ending normalization eliminates these warnings permanently by declaring the canonical line ending per file type.## Current state- No `.gitattributes` exists at the repo root (the `.gitattributes` in `src-tauri/` is for Git LFS, not line endings).- `git diff --stat HEAD` produces warnings like: `warning: in the working copy of 'src/App.tsx', LF will be replaced by CRLF the next time Git touches it` for every tracked file.- All source files are currently stored with LF endings in the repo (as confirmed by `git diff` showing no actual content diffs, only the CRLF warning).The repo has:- TypeScript/TSX/CSS files in `src/`- Rust files in `src-tauri/`- TOML files in `src-tauri/`- JSON files (`package.json`, `tsconfig.json`, etc.)- PowerShell scripts in `scripts/`- Markdown files## Commands you will need| Purpose   | Command                  | Expected on success ||-----------|--------------------------|---------------------|| Git check | `git diff --stat HEAD`   | no CRLF warnings    || Tests     | `bun run test`           | all pass            || Rust tests| `& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --manifest-path src-tauri/Cargo.toml` | all pass |## Scope**In scope**:- `.gitattributes` (create at repo root)**Out of scope**:- Any source code changes- `.gitignore` changes- Renormalization of existing files (a separate `git add --renormalize .` commit, which can be done later)## Git workflow- Branch: `advisor/004-gitattributes-line-endings`- Commit: `Add .gitattributes to normalize line endings`- Do NOT push or open a PR.## Steps### Step 1: Create `.gitattributes`Create a file at the repo root `.gitattributes` with the following content:```* text=auto eol=lf*.ps1 text eol=crlf*.bat text eol=crlf*.rs text eol=lf*.toml text eol=lf*.tsx text eol=lf*.ts text eol=lf*.css text eol=lf*.html text eol=lf*.json text eol=lf*.md text eol=lf*.yml text eol=lf*.yaml text eol=lf*.lock text eol=lf*.bin binary*.exe binary*.dll binary*.woff2 binary*.png binary*.jpg binary*.jpeg binary*.zip binary*.msi binary*.sig binary```This declares:- Default: auto-detect text, store as LF- PowerShell scripts: CRLF (Windows convention)- All source: LF in repo (normalized on checkout)- Binary files: no normalization**Verify**: `Test-Path .gitattributes` → True### Step 2: Commit the change```git add .gitattributesgit commit -m "Add .gitattributes to normalize line endings"```**Verify**: `git log --oneline -1` → shows the commit### Step 3: Verify no warnings on future diffsRun `git diff --stat HEAD`. There should be no CRLF/LF warnings for tracked files. (Existing working tree files may still need renormalization; this plan does not include that step to keep it minimal.)**Verify**: `git diff --stat HEAD` → no "LF will be replaced by CRLF" warnings for files covered by `.gitattributes`## Test plan- No new test files needed — this is a git configuration change.- Verification: `git diff --stat HEAD` produces no line-ending warnings.- Verification: `bun run test` and `cargo test --manifest-path src-tauri/Cargo.toml` still pass (no source changes).## Done criteria- [ ] `.gitattributes` exists at repo root- [ ] `git log --oneline -1` shows the add commit- [ ] `git diff --stat HEAD` shows no CRLF warnings for source files- [ ] No source code files modified- [ ] `bun run test` and `cargo test` still pass- [ ] `plans/README.md` status row updated## STOP conditionsStop and report back (do not improvise) if:- `.gitattributes` already exists at the repo root (may have been added since this plan was written).- Adding `.gitattributes` causes `git status` to show unexpected renormalization diffs for binary files (unlikely with the `binary` declarations).## Maintenance notes- If renormalization is desired in the future, run `git add --renormalize . && git commit -m "Renormalize line endings"` as a separate commit.- When adding new binary file types (models, audio), add them to `.gitattributes` with the `binary` attribute.