# Implementation Plans

Generated by the improve skill on 2026-06-10. Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before starting,
honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Add React error boundary to prevent full UI crash | P1 | S | — | DONE |
| 002  | Sanitize polish error messages to avoid API key name exposure | P1 | S | — | DONE |
| 003  | Fix gitignored release artifacts that are tracked in the repo | P2 | S | — | REJECTED (release/ already untracked when executed) |
| 004  | Add .gitattributes to normalize line endings and eliminate CRLF warnings | P2 | S | — | DONE |
| 005  | Make FTS search index update atomic within transactions | P2 | S | — | DONE |
| 006  | Make retention sweeper thread gracefully shut down on app exit | P2 | S | — | DONE |
| 007  | Extract App.tsx panels into separate component files | P1 | M | — | IN PROGRESS (6 of 8 panels extracted; Onboarding + OverlayWindow remain) |
| 008  | Move Tauri command handlers off the main thread for polish operations | P2 | M | — | TODO |
| 009  | Replace full AppSnapshot refresh with targeted state updates | P3 | M | 007 | TODO |
| 010  | Add frontend unit tests for core App behavior | P3 | L | 007 | TODO |

## Dependency notes

- **007 → 009**: Plan 009 (targeted state updates) modifies `App.tsx` state management extensively. Plan 007 extracts panels from `App.tsx` first. If 009 runs before 007, it would create massive merge conflicts. Execute 007 first.
- **007 → 010**: Plan 010 (frontend tests) tests `HistoryPanel` and `SettingsPanel` as separate components. They don't exist yet as separate files until 007 extracts them. Execute 007 first; 010 can proceed once the extraction is done.
- **001, 002, 003, 004, 005, 006, 008** are independent and can run in any order.

## Recommended parallelization

- **Batch 1** (independent, S effort): 001, 002, 003, 004, 005, 006 — can all run simultaneously
- **Batch 2** (independent, M effort): 007, 008 — can run simultaneously if independent executors are available
- **Batch 3** (depends on 007): 009, 010 — run after 007 is DONE

## Findings considered and rejected

- **SSRF via polish endpoint URLs**: The `polish_endpoint` setting allows arbitrary HTTP endpoints. This is by-design for local Ollama compatibility; restricting it would break the product. The env-var-based API key approach (not stored in DB) is the right mitigation.
- **No CI/CD pipeline**: The repo has no GitHub Actions or other CI configuration. Adding CI is out of scope for this audit (would be a DX category finding), but flagged as a follow-up.
- **Cargo.lock committed**: The `src-tauri/Cargo.lock` is committed (and should be for an application — it's not a library). This is correct and not a finding.
- **Large AppSnapshot on every refresh**: This is the same finding as 009 (targeted state updates) — not a separate finding.