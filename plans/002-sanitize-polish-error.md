# Plan 002: Sanitize polish error messages to avoid leaking API key hints> **Executor instructions**: Follow this plan step by step. Run every> verification command and confirm the expected result before moving to the> next step. If anything in the "STOP conditions" section occurs, stop and> report — do not improvise. When done, update the status row for this plan> in `plans/README.md`.>> **Drift check (run first)**: `git diff --stat 1bb6795..HEAD -- src-tauri/src/services/polish.rs`> If any in-scope file changed since this plan was written, compare the> "Current state" excerpts against the live code before proceeding; on a> mismatch, treat it as a STOP condition.## Status- **Priority**: P1- **Effort**: S- **Risk**: LOW- **Depends on**: none- **Category**: security- **Planned at**: commit `1bb6795`, 2026-06-10## Why this mattersThe `polish.rs` error message for a missing API key includes the environment variable name (`WIND_SPEAK_POLISH_API_KEY`) in a string that could be logged, displayed in the UI, or persisted as a `RuntimeEvent`. While the value is never exposed, explicitly naming the env var in user-facing error messages gives attackers a targeting hint. The existing test at `src-tauri/src/services/polish.rs:276-283` checks for the word "disabled" in the error, so any change must preserve that test.## Current state- `src-tauri/src/services/polish.rs:8` — the constant:  ```rust  const OPENAI_API_KEY_ENV: &str = "WIND_SPEAK_POLISH_API_KEY";  ```- `src-tauri/src/services/polish.rs:104-109` — the error message:  ```rust  let api_key = std::env::var(OPENAI_API_KEY_ENV)      .map(|value| value.trim().to_string())      .unwrap_or_default();  if api_key.is_empty() {      return Err(anyhow!(          "{OPENAI_API_KEY_ENV} must be set before using the OpenAI-compatible polish provider"      ));  }  ```- `src-tauri/src/services/polish.rs:276-283` — the test that checks the "disabled" error:  ```rust  #[test]  fn disabled_provider_rejects_polish_requests() {      let mut settings = AppSettings::default();      settings.polish_provider = PolishProviderKind::Disabled;      let error = polish_text(&settings, "hello world", None).expect_err("disabled");      assert!(error.to_string().contains("disabled"));  }```The repo's test convention: `cargo test --manifest-path src-tauri/Cargo.toml`.## Commands you will need| Purpose   | Command                  | Expected on success ||-----------|--------------------------|---------------------|| Rust tests| `& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --manifest-path src-tauri/Cargo.toml` | 53 passed, 0 failed, 2 ignored |## Scope**In scope**:- `src-tauri/src/services/polish.rs` — change the error message for missing API key**Out of scope**:- `src-tauri/src/services/diagnostics.rs` — the diagnostics log system is separate- `src/App.tsx` — UI error display is out of scope- Any other error messages in other files## Git workflow- Branch: `advisor/002-sanitize-polish-error`- Commit style: imperative (e.g. "Sanitize polish error messages to avoid API key name exposure")- Do NOT push or open a PR.## Steps### Step 1: Replace the specific env var name in the error messageIn `src-tauri/src/services/polish.rs`, change lines 104-109 from:```rustlet api_key = std::env::var(OPENAI_API_KEY_ENV)    .map(|value| value.trim().to_string())    .unwrap_or_default();if api_key.is_empty() {    return Err(anyhow!(        "{OPENAI_API_KEY_ENV} must be set before using the OpenAI-compatible polish provider"    ));}```to:```rustlet api_key = std::env::var(OPENAI_API_KEY_ENV)    .map(|value| value.trim().to_string())    .unwrap_or_default();if api_key.is_empty() {    return Err(anyhow!(        "API key not configured for the OpenAI-compatible polish provider. Set the required environment variable."    ));}```This removes the env var name from user-facing error text while keeping the constant for the actual lookup.**Verify**: `& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --manifest-path src-tauri/Cargo.toml` → 53 passed, 0 failed, 2 ignored### Step 2: Add a test for the missing-key error messageAdd a new test in `src-tauri/src/services/polish.rs` inside the `#[cfg(test)] mod tests` block (after line 295). The test verifies the OpenAI-compatible provider returns an error that does NOT contain the env var name:```rust#[test]fn openai_compatible_provider_rejects_missing_api_key() {    let mut settings = AppSettings::default();    settings.polish_provider = PolishProviderKind::OpenAiCompatible;    settings.polish_endpoint = "http://127.0.0.1:1/unreachable".to_string();    let error = polish_text(&settings, "hello world", None).expect_err("missing key");    let message = error.to_string();    assert!(!message.contains("WIND_SPEAK"), "error message must not contain the env var name: {message}");}```Note: This test may fail with a connection error rather than the missing-key error if the env var happens to be set in the test environment. That's fine — the assertion on `!message.contains("WIND_SPEAK")` still validates the security property.**Verify**: `& "$env:USERPROFILE\.cargo\bin\cargo.exe" test --manifest-path src-tauri/Cargo.toml` → all pass (54 passed, 0 failed, 2 ignored)## Test plan- New test: `openai_compatible_provider_rejects_missing_api_key` in `src-tauri/src/services/polish.rs`  - Verifies error message does not contain the env var name- Existing test: `disabled_provider_rejects_polish_requests` — must still pass (unchanged)- Verification: `cargo test --manifest-path src-tauri/Cargo.toml` → all pass## Done criteria- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` exits 0; new test passes- [ ] `grep -n "WIND_SPEAK_POLISH_API_KEY" src-tauri/src/services/polish.rs` only shows the constant declaration (line 8) and the `std::env::var` call (line 104), NOT the error message string- [ ] No files outside `src-tauri/src/services/polish.rs` are modified- [ ] `plans/README.md` status row updated## STOP conditionsStop and report back (do not improvise) if:- The code at `src-tauri/src/services/polish.rs:104-109` doesn't match the excerpt (drift).- The `disabled_provider_rejects_polish_requests` test starts failing after the change.- The `OPENAI_API_KEY_ENV` constant is used elsewhere in error constructions (grep for it).## Maintenance notes- If new polish providers are added, their error messages for missing configuration should follow the same pattern: no env var names in user-facing strings.- Reviewers should check that the diagnostics log (a separate path) still gets enough info for debugging — the constant `OPENAI_API_KEY_ENV` is still usable in `diagnostics::log()` calls since those go to the local event buffer, not the user UI.