# Plan 003: Fix gitignored release artifacts that are tracked in the repo> **Executor instructions**: Follow this plan step by step. Run every> verification command and confirm the expected result before moving to the> next step. If anything in the "STOP conditions" section occurs, stop and> report — do not improve — do not improvise. When done, update the status row for this plan> in `plans/README.md`.## Status- **Priority**: P2- **Effort**: S- **Risk**: LOW- **Depends on**: none- **Category**: dx- **Planned at**: commit `1bb6795`, 2026-06-10## Why this mattersThe `.gitignore` lists `release/` as an ignored pattern, yet the `release/` directory contains committed binary artifacts (MSI, EXE, signature files, `latest.json`). This creates two problems: (1) `git status` and `git diff` always show noisy CRLF/LF warnings for these tracked-but-ignored files, and (2) future release builds that put files in `release/` will be silently ignored by git, which is the intended behavior — but because the current files are already tracked, they override that intent. The fix is to untrack them so gitignore takes effect, while keeping them locally available.## Current state- `.gitignore:4` — `release/`- `release/` directory contains committed large binary files:  - `atmospeak_0.1.9_x64_en-US.msi`  - `atmospeak_0.1.9_x64_en-US.msi.sig`  - `atmospeak_0.1.9_x64-setup.exe`  - `atmospeak_0.1.9_x64-setup.exe.sig`  - `atmospeak_0.1.9_x64-portable.zip`  - `SHA256SUMS.txt`  - `latest.json`These are tracked despite matching the gitignore rule because they were committed before `release/` was added to `.gitignore`.## Commands you will need| Purpose   | Command                  | Expected on success ||-----------|--------------------------|---------------------|| Git check | `git ls-files release/`   | lists the tracked files || Git rm    | `git rm --cached -r release/` | removes from tracking |## Scope**In scope**:- Git index (untracking release/ files)- `.gitignore` — no changes needed, already correct**Out of scope**:- `scripts/package-release.ps1` — the build script that outputs to `release/`- Any source code changes- Actually deleting the local `release/` directory## Git workflow- Branch: `advisor/003-gitignore-release`- Commit: `Remove tracked release artifacts from git index (already gitignored)`- Do NOT push or open a PR.## Steps### Step 1: Verify which release files are trackedRun `git ls-files release/` and confirm it lists the 7 files above. If it lists no files, they may have already been untracked — STOP and report.**Verify**: `git ls-files release/` → lists at least 3 files### Step 2: Untrack the release directoryRun:```git rm --cached -r release/```This removes the files from the git index while keeping them on disk. The `.gitignore` entry for `release/` will now take effect.**Verify**: `git ls-files release/` → no output (empty)### Step 3: Commit the change```git add -Agit commit -m "Remove tracked release artifacts from git index (already gitignored)"```**Verify**: `git ls-files release/` → no output; files still exist on disk (`Test-Path release/latest.json` → True)## Test plan- Verification: `git ls-files release/` returns empty (files are no longer tracked)- Verification: `Test-Path release/latest.json` → True (files still exist locally)- Verification: `git status` should no longer show CRLF warnings for `release/` files## Done criteria- [ ] `git ls-files release/` returns empty- [ ] `Test-Path release/latest.json` → True (local copy preserved)- [ ] Commit `Remove tracked release artifacts from git index` exists- [ ] No source code files modified- [ ] `plans/README.md` status row updated## STOP conditionsStop and report back (do not improvise) if:- `git ls-files release/` returns empty before Step 1 — the files may already be untracked.- Any file in `release/` is tracked that shouldn't be committed as source (e.g., a `.gitkeep` is fine).- The `git rm --cached` step attempts to delete files from disk (it shouldn't — `--cached` prevents that).## Maintenance notes- The `scripts/package-release.ps1` script outputs to `release/`, which is correct — git will ignore the output.- Future releases should use GitHub Releases or an artifact store rather than committing binaries. The `release/` directory should remain gitignored.- If someone needs to track `latest.json` specifically, add a negation rule: `!release/latest.json` in `.gitignore`.