## SummaryThis branch contains 7 of the 10 advisor plans, applied as a series of self-contained commits.### Plans completed- **#001 — React error boundary** (`f30ac5e`): New `ErrorBoundary` component with class-component pattern; wraps both the hub and overlay windows so any render error shows a recovery UI instead of a white screen. 3 new tests.- **#002 — Sanitize polish error messages** (`13b0135`): Removes the env var name from user-facing error messages in the OpenAI-compatible polish provider. New test asserts the env var name never appears in error text.- **#003 — Untrack release artifacts** (rejected): Already untracked; recorded as rejected in `plans/README.md`.- **#004 — Add `.gitattributes`** (`4b8ebaf`): Normalizes line endings for source files, marks binary files; eliminates the LF/CRLF warning spam on every `git status`.- **#005 — Atomic FTS updates** (`bc02faa`): Wraps `insert_session`, `update_session_cleaned_text`, and `delete_transcripts_older_than_minutes` in `unchecked_transaction()` so the FTS index and main table stay consistent.- **#006 — Cancellable retention sweeper** (`30208f5`): Adds `Arc<AtomicBool>` cancel token; sweeper checks every 1s instead of one 5-min sleep, allowing clean shutdown within 1 second.- **#007 — Extract App.tsx panels** (in progress, 6/8): DictionaryPanel, SnippetPanel, HomePanel, HistoryPanel, AdvancedPanel, SettingsPanel, plus shared components (EditableRow, PanelTitle, EmptyState, MetricCard, ToggleRow, RuntimeEventList) extracted into separate files. Onboarding and OverlayWindow remain.### Verification- `npx tsc --noEmit`: exit 0- `bun run test`: 12 tests pass (3 new ErrorBoundary + 9 existing)- `cargo test --manifest-path src-tauri/Cargo.toml`: 54 passed, 2 ignored### Status| Plan | Title | Status ||------|-------|--------|| 001  | React error boundary | DONE || 002  | Sanitize polish error messages | DONE || 003  | Untrack release artifacts | REJECTED || 004  | Add .gitattributes | DONE || 005  | Atomic FTS updates | DONE || 006  | Cancellable retention sweeper | DONE || 007  | Extract App.tsx panels | IN PROGRESS |### Next steps- Finish plan 007 (Onboarding + OverlayWindow extraction)- Continue with plans 008, 009, 010 in subsequent PRs