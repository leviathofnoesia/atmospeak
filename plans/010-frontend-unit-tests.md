# Plan 010: Add frontend unit tests for core App behavior> **Executor instructions**: Follow this plan step by step. Run every> verification command and confirm the expected result before moving to the> next step. If anything in the "STOP conditions" section occurs, stop and> report — do not improvise. When done, update the status row for this plan> in `plans/README.md`.## Status- **Priority**: P3- **Effort**: L- **Risk**: LOW- **Depends on**: 007 (component extraction — tests on a 2800-line monolith are fragile; test on extracted components)- **Category**: tests- **Planned at**: commit `1bb6795`, 2026-06-10## Why this mattersThe frontend has only 9 tests across 2 files (`StatusLed.test.tsx` and `RecorderOverlay.test.tsx`). The core business logic — settings save/load, dictionary CRUD, snippet CRUD, session history, search, export, polish — has zero test coverage. A single regression in `api.ts` mock logic, `db/mod.rs` query construction, or state merge logic could ship undetected. This plan adds targeted unit tests for the most critical paths that currently have no coverage, using the existing test infrastructure (vitest + jsdom + @testing-library/react).## Current state- `vitest.config.ts` — configured with jsdom environment, setup at `src/test/setup.ts`- `src/test/setup.ts` — exists but content not shown (likely minimal)- `src/components/RecorderOverlay.test.tsx` — 8 tests, covers overlay rendering- `src/components/StatusLed.test.tsx` — 1 test, covers LED renderingThe mock API layer in `src/lib/api.ts` is well-structured with a `hasTauriRuntime()` check and comprehensive fallbacks. In browser/test mode, all API functions return mock data, making the components testable without a Tauri backend.## Commands you will need| Purpose   | Command                  | Expected on success ||-----------|--------------------------|---------------------|| Tests     | `bun run test`           | all pass            || Typecheck | `npx tsc --noEmit`       | exit 0              |## Scope**In scope** (create):- `src/lib/api.test.ts` — tests for the mock API layer- `src/components/HistoryPanel.test.tsx` — tests for history panel rendering- `src/components/SettingsPanel.test.tsx` — tests for settings panel rendering**Out of scope**:- E2E tests (Playwright is configured but out of scope for this plan)- Rust tests (already covered at 53 passing)- `src/App.test.tsx` — too large to test meaningfully before extraction## Git workflow- Branch: `advisor/010-frontend-unit-tests`- Commit style: "Add unit tests for API layer and core panels"- Do NOT push or open a PR.## Steps### Step 1: Review test setup and add api.ts testsCreate `src/lib/api.test.ts` with tests for the mock API layer that runs in both Tauri and browser mode. The key insight: `hasTauriRuntime()` returns `false` in test (jsdom) mode, so all API functions use their mock fallbacks.Tests to write:1. `hasTauriRuntime()` returns `false` in test environment2. `getAppSnapshot()` returns mock data with expected shape (settings, dictionary, snippets, sessions, stats)3. `upsertDictionaryEntry()` adds a new entry to the mock dictionary4. `deleteDictionaryEntry()` removes an entry from the mock dictionary5. `upsertSnippet()` adds a new snippet6. `deleteSnippet()` removes a snippet7. `polishTranscript()` returns a mock polish result with the specified style8. `searchSessions()` filters mock sessions by query9. `exportSession()` returns formatted export text10. `listRecentApps()` aggregates mock session data by app name11. `getModelStatus()` returns mock model status with `ready: true`12. `getModelInventory()` returns the expected model inventory shape13. `checkForUpdates()` returns `available: false` in browser mode14. `startRecording()` returns a mock recording started event15. `stopRecording()` returns a mock dictation resultFollow the pattern in `src/components/RecorderOverlay.test.tsx` for imports and setup.**Verify**: `bun run test` → all new tests pass### Step 2: Add HistoryPanel tests (after plan 007 extraction)This step requires that `HistoryPanel` has been extracted to its own file (plan 007). If it hasn't been extracted yet, STOP and report — do not test the inline monolith.Create `src/components/HistoryPanel.test.tsx` with:1. Renders session list when sessions are provided2. Shows "No transcripts yet" when sessions array is empty3. Calls onCopy when copy button is clicked for a session4. Calls onInject when inject button is clicked for a session5. Handles polish action when polish button is clicked (and polishingSessionId matches)Use the mock snapshot from `api.ts` as test data. Render `HistoryPanel` with the required props.**Verify**: `bun run test` → all pass### Step 3: Add SettingsPanel tests (after plan 007 extraction)This step requires that `SettingsPanel` has been extracted to its own file (plan 007).Create `src/components/SettingsPanel.test.tsx` with:1. Renders with default settings2. Displays the current hotkey in the shortcut section3. Shows microphone options4. Calls onSave when settings are modified**Verify**: `bun run test` → all pass### Step 4: Verify typecheck and full test suite**Verify**: `npx tsc --noEmit` → exit 0**Verify**: `bun run test` → all pass (9 existing + ~20 new)## Test planThis plan IS the test plan. The tests written here become the test coverage baseline. They should:- Use the existing vitest + jsdom + @testing-library/react setup- Follow the import pattern from `RecorderOverlay.test.tsx`- Test behavior (what the user sees), not implementation details- Work in both Tauri and browser mock mode (using `hasTauriRuntime()` checks where needed)## Done criteria- [ ] `src/lib/api.test.ts` exists with 14+ tests- [ ] `src/components/HistoryPanel.test.tsx` exists with 5+ tests (if extraction done)- [ ] `src/components/SettingsPanel.test.tsx` exists with 4+ tests (if extraction done)- [ ] `bun run test` all pass- [ ] `npx tsc --noEmit` exits 0- [ ] Total test count increased from 9 to 28+- [ ] `plans/README.md` status row updated## STOP conditionsStop and report back (do not improvise) if:- `HistoryPanel` and `SettingsPanel` are still inline in `App.tsx` (plan 007 not yet executed). Do not write tests for components still inside App.tsx — they'll be fragile and need rewriting after extraction.- The `vitest.config.ts` or `src/test/setup.ts` setup doesn't work with the new test files.- Any mock API function returns unexpected shapes (check by importing `defaultSettings` from `api.ts` and verifying the mock structure).## Maintenance notes- When adding new API functions or changing return types (as in plan 009), update the corresponding tests.- When new Tauri commands are added, add both a Rust unit test and a frontend mock test.- The mock API in `api.ts` should be the single source of truth for test data shapes. If the real API diverges, the mocks must be updated to match.- A future plan should add Playwright E2E tests for the full dictation flow.