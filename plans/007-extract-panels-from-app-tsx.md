# Plan 007: Extract App.tsx panels into separate component files> **Executor instructions**: Follow this plan step by step. Run every> verification command and confirm the expected result before moving to the> next step. If anything in the "STOP conditions" section occurs, stop and> report — do not improvise. When done, update the status row for this plan> in `plans/README.md`.## Status- **Priority**: P1- **Effort**: M- **Risk**: MED- **Depends on**: 001 (error boundary — components should be in separate files before adding the boundary wrap)- **Category**: tech-debt- **Planned at**: commit `1bb6795`, 2026-06-10## Why this matters`App.tsx` is 2800+ lines with all panels, the overlay window, onboarding, state management, and event handlers in one file. This makes it slow to edit, hard to test, and forces every developer to understand the entire file before changing anything. The panels (`HomePanel`, `HistoryPanel`, `DictionaryPanel`, `SnippetPanel`, `SettingsPanel`, `AdvancedPanel`, `Onboarding`) are self-contained React function components with clear prop interfaces. Extracting them into separate files under `src/components/` enables independent testing, faster HMR during development, and prepares the codebase for the planned feature work (T1-04 model downloader, T1-12 dictionary auto-learn, etc.).## Current state- `src/App.tsx` — 2800+ lines, contains all panel components as inline function components- `src/components/RecorderOverlay.tsx` — already extracted, serves as the pattern- `src/components/RecorderOverlay.test.tsx` — existing test file, serves as test pattern- `src/types/dictation.ts` — shared types already in a separate file- `src/lib/api.ts` — all API calls already in a separate fileExcerpt of current inline panel structure (approximate line ranges in `App.tsx`):- `HomePanel` — lines ~1554-1650- `HistoryPanel` — lines ~1089-1129 (render), but the full component is much larger with search/filter UI- `DictionaryPanel` — inline in the hub JSX- `SnippetPanel` — inline in the hub JSX- `SettingsPanel` — inline in the hub JSX- `AdvancedPanel` — inline in the hub JSX- `Onboarding` — lines ~966-995 (render call), full component earlier in the file- `OverlayWindow` — lines ~1249-1551The repo's existing extraction pattern: `src/components/RecorderOverlay.tsx` is imported in `App.tsx` as `import { RecorderOverlay } from "./components/RecorderOverlay"`. Follow this pattern.## Commands you will need| Purpose   | Command                  | Expected on success ||-----------|--------------------------|---------------------|| Typecheck | `npx tsc --noEmit`       | exit 0, no errors   || Tests     | `bun run test`           | all pass            || Dev check | `bun run build`          | exit 0              |## Scope**In scope** (files to create or modify):- `src/components/HomePanel.tsx` (create)- `src/components/HistoryPanel.tsx` (create)- `src/components/DictionaryPanel.tsx` (create)- `src/components/SnippetPanel.tsx` (create)- `src/components/SettingsPanel.tsx` (create)- `src/components/AdvancedPanel.tsx` (create)- `src/components/Onboarding.tsx` (create)- `src/components/OverlayWindow.tsx` (create)- `src/App.tsx` (modify — remove extracted components, add imports)**Out of scope**:- `src/components/RecorderOverlay.tsx` — already extracted- `src/App.css` — no style changes- `src/lib/api.ts` — no API changes- `src/types/dictation.ts` — no type changes- `src-tauri/` — no Rust changes- Any behavioral changes — this is a pure refactoring, the app must work identically## Git workflow- Branch: `advisor/007-extract-panels`- Commit per panel extraction (7-8 commits, matching repo style): e.g., "Extract HomePanel into separate component"- Do NOT push or open a PR.## Steps### Step 1: Extract OverlayWindow componentThe `OverlayWindow` function component (lines ~1249-1551 in `App.tsx`) is the most self-contained. Extract it first.1. Create `src/components/OverlayWindow.tsx`2. Move the `OverlayWindow` function and its local types (`OverlayStatePayload`) and helper constants (`overlaySizeOrder`, `overlaySizeDimensions`, `recordingLevelPollMs`, `recordingFftPollMs`, `recordingLevelCommitMs`, `recordingLevelDelta`) into the new file3. Import everything `OverlayWindow` needs from `src/lib/api.ts`, `src/types/dictation.ts`, `src/components/RecorderOverlay.tsx`, and React4. Export `OverlayWindow` as a named export5. In `App.tsx`, replace the inline `OverlayWindow` with `import { OverlayWindow } from "./components/OverlayWindow"` 6. Remove the moved code from `App.tsx`**Verify**: `npx tsc --noEmit` → exit 0, no errors### Step 2: Extract Onboarding component1. Create `src/components/Onboarding.tsx`2. Move the `Onboarding` function component (with its props interface) into the new file3. Import its dependencies (api functions, types, lucide-react icons)4. Export `Onboarding` as a named export5. In `App.tsx`, add `import { Onboarding } from "./components/Onboarding"` and remove the inline definition**Verify**: `npx tsc --noEmit` → exit 0### Step 3: Extract HomePanel component1. Create `src/components/HomePanel.tsx`2. Move the `HomePanel` function and its `HomePanelProps` interface3. Import dependencies4. Export as named export5. In `App.tsx`, add import and remove inline definition**Verify**: `npx tsc --noEmit` → exit 0### Step 4: Extract HistoryPanel component1. Create `src/components/HistoryPanel.tsx`2. Move `HistoryPanel` and `HistoryPanelProps`3. Import dependencies4. Export and update `App.tsx`**Verify**: `npx tsc --noEmit` → exit 0### Step 5: Extract DictionaryPanel and SnippetPanelThese two are simpler panels. Extract each into its own file.1. Create `src/components/DictionaryPanel.tsx` and `src/components/SnippetPanel.tsx`2. Move each panel function and its props interface3. Both import `DictionaryEntry`, `Snippet` from types and the upsert/delete functions from api4. Update `App.tsx`**Verify**: `npx tsc --noEmit` → exit 0### Step 6: Extract SettingsPanel and AdvancedPanelThese are the two remaining panels. Extract each.1. Create `src/components/SettingsPanel.tsx` and `src/components/AdvancedPanel.tsx`2. Move each panel and its props interface3. `SettingsPanel` depends on many API functions and state — all passed as props4. Update `App.tsx`**Verify**: `npx tsc --noEmit` → exit 0### Step 7: Clean up App.tsxAfter all extractions:1. Remove unused imports from `App.tsx` (icons that only appeared in extracted panels, types only used in extracted props)2. Remove any remaining helper constants that moved to their new homes3. Verify `App.tsx` now only contains the `App` component, event listeners, state hooks, and the hub layout rendering**Verify**: `npx tsc --noEmit` → exit 0**Verify**: `bun run test` → all pass**Verify**: `bun run build` → exit 0## Test plan- Existing tests in `src/components/RecorderOverlay.test.tsx` and `src/components/StatusLed.test.tsx` must continue passing.- No new tests are required for this pure refactoring — the components are not changing behavior, just location.- Manual verification: the app renders identically (same panels, same behavior).- Verification: `npx tsc --noEmit`, `bun run test`, `bun run build` all pass.## Done criteria- [ ] `npx tsc --noEmit` exits 0- [ ] `bun run test` exits 0- [ ] `bun run build` exits 0- [ ] `src/components/HomePanel.tsx`, `HistoryPanel.tsx`, `DictionaryPanel.tsx`, `SnippetPanel.tsx`, `SettingsPanel.tsx`, `AdvancedPanel.tsx`, `Onboarding.tsx`, `OverlayWindow.tsx` all exist- [ ] `App.tsx` imports all extracted components from `./components/`- [ ] `wc -l src/App.tsx` is significantly reduced from the original ~2800 lines (target: under 500 lines for the remaining App component)- [ ] No behavioral changes — same panels render with same props- [ ] `plans/README.md` status row updated## STOP conditionsStop and report back (do not improvise) if:- The line ranges in this plan don't match the actual code (drift since 1bb6795).- A panel component uses state or refs from `App.tsx` that can't be cleanly passed as props without major refactoring. If this happens, report which component and what state it needs — a follow-up plan for custom hooks may be needed.- `npx tsc --noEmit` fails after any extraction step and the error can't be resolved by adding the correct imports.## Maintenance notes- After this extraction, each panel can be independently tested with `@testing-library/react`.- Future feature work (T1-04 model downloader, T1-12 dictionary auto-learn) should add new props to the relevant panel component rather than adding state to `App.tsx`.- If a panel starts needing shared state beyond what `App.tsx` passes as props, consider a React context or a lightweight state management solution rather than reverting to inline definitions.- Reviewers should check that no behavior is accidentally lost during extraction — especially event listeners and `useEffect` hooks that interact with Tauri events.